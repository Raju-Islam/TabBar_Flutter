import 'package:flutter/material.dart';
class Messageapage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: new Center(
          child: new Text("Message"),
        )
    );
  }
}
